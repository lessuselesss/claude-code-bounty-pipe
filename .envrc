#!/usr/bin/env bash

# Load the Nix development environment
use flake

# Additional environment setup for bounty pipeline
export BOUNTY_PIPE_PROJECT_ROOT="$(pwd)"

# Ensure XDG directories exist
mkdir -p ~/.cache/bounty-pipe
mkdir -p ~/.local/share/bounty-pipe/work

# Set up Deno cache in XDG location
export DENO_DIR="$XDG_CACHE_HOME/deno"

# Add project-specific environment hints
echo "💡 Bounty Pipeline environment loaded!"
echo "   Run 'deno task --help' to see available commands"